RewriteEngine On

# Handle API routes
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^api/(.*)$ api-test.php [QSA,L]

# Handle all other requests to index.html for SPA
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.html [QSA,L]
