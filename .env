# Use "/" for root-domain deployments, or "/myapp/" for subfolder deployments
VITE_PUBLIC_BASE=/

# API Configuration - Use relative path for same-origin deployment
# Leave empty to use relative URLs (same origin)
VITE_API_BASE_URL=